// Invisible collision shell for one chapel nave side wall (kit model, full
// 16.6 m built length, rising to the 7.5 m springline — the vault above is
// chapel_roof's job). The visual is the always-rendered chapel zone prop,
// so no ShapeGroup; the hitbox keeps the spawn-centred convention.
(
    components: {
        "Transform": (),
        "Hitbox":    (shape: Aabb(half_extents: (8.3, 3.75, 0.3))),
        "Solid":     (),
        "Anchored":  (),
    },
)
