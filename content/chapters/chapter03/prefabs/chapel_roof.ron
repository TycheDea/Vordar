// Invisible collision slab for the chapel's intact west-half barrel vault —
// the east half is collapsed, open to sky. Spawned aloft (y = 10.2) at the
// vault crown, far above the movement plane, so its solidity is inert.
(
    components: {
        "Transform": (),
        "Hitbox":    (shape: Aabb(half_extents: (4.0, 0.3, 3.5))),
        "Solid":     (),
        "Anchored":  (),
    },
)
