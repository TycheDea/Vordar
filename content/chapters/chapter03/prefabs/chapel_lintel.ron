// Invisible collision shell for the chapel wall above the door opening.
// Spawned aloft (y = 5.35) so the spawn-centred hitbox spans y 3.2..7.5 —
// door head to wall top; the passage below stays open.
(
    components: {
        "Transform": (),
        "Hitbox":    (shape: Aabb(half_extents: (0.3, 2.15, 1.2))),
        "Solid":     (),
        "Anchored":  (),
    },
)
