// Invisible collision shell for one chapel east-wall piece flanking the
// door; two placements leave the 2.4 m opening between them (a Hitbox is one
// Aabb centred on the spawn point, so the opening comes from placement).
(
    components: {
        "Transform": (),
        "Hitbox":    (shape: Aabb(half_extents: (0.3, 3.75, 1.3))),
        "Solid":     (),
        "Anchored":  (),
    },
)
