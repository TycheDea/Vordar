// Invisible collision shell closing the chapel nave's west end. The built
// apse is a fan of segments reaching ~3 m further west — deliberately left
// uncollided (../footprints.ron chapel tolerance); this flat wall blocks the
// apse mouth instead.
(
    components: {
        "Transform": (),
        "Hitbox":    (shape: Aabb(half_extents: (0.3, 3.75, 3.8))),
        "Solid":     (),
        "Anchored":  (),
    },
)
