Read docs/reviews/audit-networking-2026-07-11.md. Extract the complete text
of finding 1 (title through Path, verbatim). Spawn one finding-worker
subagent with that text as its task. When it returns, show me its final
report and `git diff --stat`. "Nothing else" binds you, the orchestrator,
not the worker: you make no file edits and run no fixes yourself — the
worker does all implementation work, and it must attempt the finding, not
decline it.
